import styled from 'styled-components'
import { textStyle, fontSize, space, layout, color, typography } from 'styled-system'

const Text = styled.div`
  ${textStyle}
  ${typography}
  ${space}
  ${layout}
  ${fontSize}
  ${color}
`

export default Text
